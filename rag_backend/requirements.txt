# =============================================================================
# PRIVATE CORPORATE AI — Dipendenze Python RAG Backend
# Compatibilità: Python 3.11+
# Tutte le librerie sono Open Source (Apache 2.0 / MIT)
# Installazione: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# WEB FRAMEWORK — API REST
# -----------------------------------------------------------------------------
fastapi==0.115.5                   # Framework API async (MIT)
uvicorn[standard]==0.32.1          # Server ASGI ad alte prestazioni (BSD)
python-multipart==0.0.17           # Upload file multipart (Apache 2.0)
pydantic==2.10.3                   # Validazione dati e settings (MIT)
pydantic-settings==2.7.0           # Gestione configurazione da .env (MIT)

# -----------------------------------------------------------------------------
# LANGCHAIN — Orchestrazione RAG Pipeline
# -----------------------------------------------------------------------------
langchain==0.3.13                  # Framework principale (MIT)
langchain-community==0.3.13        # Integrazioni community (MIT)
langchain-ollama==0.2.2            # Integrazione nativa Ollama (MIT)
langchain-qdrant==0.2.0            # Integrazione nativa Qdrant (MIT)
langchain-text-splitters==0.3.3    # Text chunking (MIT)

# -----------------------------------------------------------------------------
# VECTOR DATABASE CLIENT
# -----------------------------------------------------------------------------
qdrant-client==1.12.1              # Client Python Qdrant (Apache 2.0)

# -----------------------------------------------------------------------------
# DOCUMENT LOADERS — Supporto formati documentali
# -----------------------------------------------------------------------------
pypdf==5.1.0                       # Parsing PDF (BSD)
python-docx==1.1.2                 # Parsing DOCX Word (MIT)
unstructured[local-inference]==0.16.10  # Parsing generico documenti (Apache 2.0)
openpyxl==3.1.5                    # Parsing XLSX Excel (MIT)
python-pptx==1.0.2                 # Parsing PPTX PowerPoint (MIT)

# -----------------------------------------------------------------------------
# EMBEDDINGS & ML UTILITIES
# -----------------------------------------------------------------------------
numpy==1.26.4                      # Calcolo numerico (BSD)
sentence-transformers==3.3.1       # Modelli embedding locali fallback (Apache 2.0)

# -----------------------------------------------------------------------------
# HTTP CLIENT & UTILITIES
# -----------------------------------------------------------------------------
httpx==0.28.1                      # Client HTTP async (BSD)
aiofiles==24.1.0                   # I/O asincrono file (Apache 2.0)
python-dotenv==1.0.1               # Caricamento variabili .env (BSD)
tenacity==9.0.0                    # Retry logic con backoff esponenziale (Apache 2.0)
urllib3<2.0.0                      # Forza versione v1 per compatibilità con requests
charset-normalizer<3.4.0           # Vincolo per stabilità dipendenze

# -----------------------------------------------------------------------------
# LOGGING & MONITORING
# -----------------------------------------------------------------------------
loguru==0.7.3                      # Logging avanzato e colorato (MIT)
prometheus-client==0.21.1          # Metriche Prometheus (Apache 2.0)

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
python-jose[cryptography]==3.3.0   # JWT token handling (MIT)
passlib[bcrypt]==1.7.4             # Hashing password (BSD)
